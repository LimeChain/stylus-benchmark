set dotenv-load

build:
  forge build

deploy:
  just run Deploy

verify:
  just run Verify

run SCRIPT:
  forge script script/Ed25519.s.sol:{{ SCRIPT }} \
    --rpc-url ${RPC_URL} \
    --chain ${CHAIN_ID} \
    --private-key ${PRIVATE_KEY} \
    --broadcast
